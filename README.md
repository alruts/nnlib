
## Notes

*To-do*
* [x] Decouple "siren" initialization specific stuff from the MLP model,
which makes it so that I can also use different activations and
initializations
* [x] Finish up pirate net models
* [ ] Add pi-initialization to pirate net
* [ ] Make modular training system
    - [ ] this would mean what kind of optimizer you want to use
    - [ ] what kind of activation and initializations
    - [ ] hyperparameters
    - [ ] try out 'hyperparam optimization package' Ax
    - [ ] parallelization via either hydra or gnu parallel
